services:
  - type: web
    name: supplierhub-web
    runtime: image
    image:
      url: docker.io/narsliss/supplierhub-web:latest
    envVars:
      - key: RAILS_ENV
        value: production
      - key: SECRET_KEY_BASE
        generateValue: true
