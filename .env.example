# ===========================================
# Docker Deployment Configuration
# ===========================================
# Copy this file to .env and fill in your values
# Generate secrets with: docker run --rm ruby:3.3-slim ruby -rsecurerandom -e 'puts SecureRandom.hex(64)'

# Database (PostgreSQL)
POSTGRES_USER=supplier_hub
POSTGRES_PASSWORD=changeme_secure_password
POSTGRES_DB=supplier_hub_production

# Rails
SECRET_KEY_BASE=generate_64_byte_hex_secret_here
RAILS_ENV=production

# Redis
REDIS_URL=redis://redis:6379/0

# Encryption key for supplier credentials (generate same way as SECRET_KEY_BASE)
ATTR_ENCRYPTED_KEY=generate_64_byte_hex_secret_here

# Groq AI API (free tier) - https://console.groq.com/
GROQ_API_KEY=gsk_your_groq_api_key_here

# Chrome path for headless browser (set automatically in Docker)
# BROWSER_PATH=/usr/bin/chromium

# Sidekiq
SIDEKIQ_CONCURRENCY=5

# Optional: Port mapping (default 3000)
PORT=3000

# ===========================================
# Stripe Configuration
# ===========================================
# Get your keys from https://dashboard.stripe.com/apikeys
STRIPE_PUBLISHABLE_KEY=pk_test_your_key_here
STRIPE_SECRET_KEY=sk_test_your_key_here

# Webhook signing secret - get from Stripe Dashboard > Webhooks
STRIPE_WEBHOOK_SECRET=whsec_your_webhook_secret_here

# Create a product and price in Stripe Dashboard, then add the price ID here
# Price should be $99.00/month recurring
STRIPE_MONTHLY_PRICE_ID=price_your_price_id_here

# Optional: Trial period in days (default 14)
STRIPE_TRIAL_DAYS=14

# ===========================================
# Local Development Configuration
# ===========================================
# Uncomment these for local development (non-Docker)

# DATABASE_URL=postgres://localhost/restaurant_supplier_aggregator_development
# REDIS_URL=redis://localhost:6379/0
# RAILS_ENV=development
# RAILS_LOG_LEVEL=debug
