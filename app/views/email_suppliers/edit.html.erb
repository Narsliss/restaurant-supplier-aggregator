<div class="max-w-2xl mx-auto">
  <div class="mb-6">
    <h1 class="text-2xl font-bold text-gray-900">Edit <%= @supplier.name %></h1>
    <p class="mt-1 text-sm text-gray-500">Update this email supplier's details.</p>
  </div>

  <%= render "form", supplier: @supplier %>
</div>
