<div id="event-plan-header">
  <%# Header %>
  <div class="flex items-center justify-between mb-4">
    <div class="flex items-center space-x-3">
      <%= link_to event_plans_path, class: "text-gray-400 hover:text-gray-600 dark:hover:text-gray-300" do %>
        <svg class="w-5 h-5" fill="none" viewBox="0 0 24 24" stroke-width="1.5" stroke="currentColor">
          <path stroke-linecap="round" stroke-linejoin="round" d="M10.5 19.5L3 12m0 0l7.5-7.5M3 12h18" />
        </svg>
      <% end %>
      <div data-controller="inline-edit"
           data-inline-edit-url-value="<%= event_plan_path(event_plan) %>"
           data-inline-edit-field-value="title"
           class="flex items-center">
        <h1 class="text-xl font-heading font-bold text-gray-900 dark:text-white cursor-pointer hover:text-brand-orange transition-colors"
            data-inline-edit-target="display"
            data-action="click->inline-edit#edit"
            title="Click to rename">
          <%= event_plan.title || "New Event Plan" %>
        </h1>
        <input type="text"
               data-inline-edit-target="input"
               data-action="blur->inline-edit#save keydown->inline-edit#keydown"
               class="hidden text-xl font-heading font-bold text-gray-900 dark:text-white dark:bg-gray-900 bg-transparent border-b-2 border-brand-orange focus:outline-none w-64">
        <svg class="w-3.5 h-3.5 ml-1.5 text-gray-400 dark:text-gray-500 flex-shrink-0" fill="none" viewBox="0 0 24 24" stroke-width="1.5" stroke="currentColor">
          <path stroke-linecap="round" stroke-linejoin="round" d="m16.862 4.487 1.687-1.688a1.875 1.875 0 1 1 2.652 2.652L6.832 19.82a4.5 4.5 0 0 1-1.897 1.13l-2.685.8.8-2.685a4.5 4.5 0 0 1 1.13-1.897L16.863 4.487Zm0 0L19.5 7.125" />
        </svg>
      </div>
      <span class="inline-flex items-center rounded-full px-2.5 py-0.5 text-xs font-medium bg-yellow-100 text-yellow-800 dark:bg-yellow-900/30 dark:text-yellow-400">
        Drafting
      </span>
    </div>
    <div class="flex items-center space-x-2">
      <% if event_plan.has_menu? %>
        <%= button_to "Build Order", build_order_event_plan_path(event_plan), method: :post, class: "inline-flex items-center px-3 py-1.5 text-sm font-semibold text-white bg-brand-green rounded-md hover:bg-brand-green-dark transition-colors", data: { turbo_confirm: "This will create pending purchase orders from the menu ingredients. Continue?" } %>
      <% end %>
      <%= button_to event_plan_path(event_plan), method: :delete, data: { turbo_confirm: "Delete this event plan and all its messages?" }, class: "inline-flex items-center px-2 py-1.5 text-sm text-gray-400 hover:text-red-500 dark:text-gray-500 dark:hover:text-red-400 transition-colors", title: "Delete plan" do %>
        <svg class="w-4 h-4" fill="none" viewBox="0 0 24 24" stroke-width="1.5" stroke="currentColor">
          <path stroke-linecap="round" stroke-linejoin="round" d="m14.74 9-.346 9m-4.788 0L9.26 9m9.968-3.21c.342.052.682.107 1.022.166m-1.022-.165L18.16 19.673a2.25 2.25 0 0 1-2.244 2.077H8.084a2.25 2.25 0 0 1-2.244-2.077L4.772 5.79m14.456 0a48.108 48.108 0 0 0-3.478-.397m-12 .562c.34-.059.68-.114 1.022-.165m0 0a48.11 48.11 0 0 1 3.478-.397m7.5 0v-.916c0-1.18-.91-2.164-2.09-2.201a51.964 51.964 0 0 0-3.32 0c-1.18.037-2.09 1.022-2.09 2.201v.916m7.5 0a48.667 48.667 0 0 0-7.5 0" />
        </svg>
      <% end %>
    </div>
  </div>

  <%# Event details bar %>
  <% if event_plan.covers || event_plan.budget_per_cover || event_plan.event_details["cuisine_style"] %>
    <div class="flex flex-wrap items-center gap-x-4 gap-y-1 mb-4 px-4 py-2 bg-gray-50 dark:bg-gray-800 rounded-lg text-sm text-gray-600 dark:text-gray-400">
      <% if event_plan.event_details["cuisine_style"] %>
        <span class="inline-flex items-center rounded-full bg-brand-orange/10 text-brand-orange px-2 py-0.5 text-xs font-medium"><%= event_plan.event_details["cuisine_style"] %></span>
      <% end %>
      <% if event_plan.covers %>
        <span><strong><%= event_plan.covers %></strong> covers</span>
      <% end %>
      <% if event_plan.budget_per_cover %>
        <span>&middot; <strong>$<%= number_with_precision(event_plan.budget_per_cover, precision: 2) %></strong>/cover budget</span>
      <% end %>
      <% if event_plan.cost_per_cover %>
        <span>&middot; <strong>$<%= number_with_precision(event_plan.cost_per_cover, precision: 2) %></strong>/cover actual</span>
      <% end %>
      <% if event_plan.wines.any? %>
        <span>&middot; <strong><%= event_plan.wines.size %></strong> wines</span>
      <% end %>
    </div>
  <% end %>
</div>
