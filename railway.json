{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "DOCKERFILE",
    "dockerfilePath": "Dockerfile"
  },
  "deploy": {
    "startCommand": "sh -c 'bin/rails db:migrate && PROCESS_TYPE=web ./bin/start'",
    "restartPolicyType": "ON_FAILURE",
    "sleepApplication": false
  }
}
