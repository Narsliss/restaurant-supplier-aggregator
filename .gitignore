/.bundle
/log/*
/tmp/*
!/log/.keep
!/tmp/.keep
/storage/*
!/storage/.keep
/public/assets
.byebug_history
.env
config/master.key
config/credentials/*.key
/node_modules
/vendor/bundle
.env.docker
.env.production
.claude/

# SQLite databases
/db/*.sqlite3
/db/*.sqlite3-*

# macOS
.DS_Store

# scratch (safe to delete)
_to_delete/

# Investor/sales decks — business material, not part of the app
decks/

# Source material for the docs/training guides, not part of the app.
# Anchored to the repo root: mockups/mobile/ IS tracked, and a future Rails
# script/ directory should not be swallowed by this.
/Website_Setup_Training_Guide.docx
/mockups/mobile-first/
/mockups/onboarding/
/script/
