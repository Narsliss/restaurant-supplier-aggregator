# Known Bugs (surfaced by RSpec suite)

No outstanding bugs. The three bugs originally documented here (PlaceOrderJob
rescue hazard, PreOrderValidationService missing association, and the orphan
`mark_unavailable!` call) were fixed and their pinned specs unskipped.
